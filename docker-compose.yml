services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: clay
      POSTGRES_PASSWORD: clay
      POSTGRES_DB: clay
    ports:
      - "5432:5432"
    volumes:
      - clay_pgdata:/var/lib/postgresql/data

volumes:
  clay_pgdata:
