DATABASE_URL=postgresql://clay:clay@localhost:5432/clay

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Optional. Signs the short-lived tokens that let the PDF exporter's headless
# browser load an authenticated view. Falls back to CLERK_SECRET_KEY when
# unset, so PDF export works without extra configuration.
PDF_SIGNING_SECRET=
